# contributor: Harri Feng <harrifeng@gmail.com>
# name : Log To File
# --
import logging
import time

logging.basicConfig(
    format='[%(asctime)s %(levelname)s %(filename)s-%(lineno)d] %(message)s',
    filename='tmp-{}.log'.format(time.strftime("%Y-%m-%d")),
    level=logging.DEBUG)
$0
